services:
  dashboard:
    restart: unless-stopped
  api:
    restart: unless-stopped
    environment:
      - DATABASE_URL=postgresql+psycopg://algodesk:algodesk@postgres:5432/algodesk
  trader:
    restart: unless-stopped
    environment:
      - DATABASE_URL=postgresql+psycopg://algodesk:algodesk@postgres:5432/algodesk
